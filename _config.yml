title: IDX Signals
description: Daily IDX BUY signals — 3 strategies + Hermes Agent qualitative review
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false

# Apply our custom layout to every markdown page
defaults:
  - scope:
      path: ""
    values:
      layout: default
